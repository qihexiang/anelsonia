import { text, html, css, js } from "./text";
import { json } from "./json";
import { stream, buffer } from "./stream";
import { file } from "./file";
export const resBuilder = {
    text, html, css, js, json, stream, buffer, file
};
export default resBuilder;